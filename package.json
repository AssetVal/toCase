{
  "name": "@assetval/to-case",
  "version": "1.2.0",
  "description": "A  very small library to convert strings, while preserving the their types",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --minify --sourcemap=external --platform=node --target=node20.90 --outfile=./dist/index.js  "
  },
  "author": "Antonio Bourassa",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.19.6",
    "typescript": "^5.2.2"
  },
  "dependencies": {}
}
